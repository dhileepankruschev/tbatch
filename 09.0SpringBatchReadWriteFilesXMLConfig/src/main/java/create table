create database test;

use test;

create table product (
	id varchar(20) NOT NULL PRIMARY KEY, 
	NAME varchar(50), 
	DESCRIPTION varchar(200), 
	PRICE decimal
);

create table product (id varchar(20) NOT NULL PRIMARY KEY, NAME varchar(50), DESCRIPTION varchar(200), PRICE decimal);

